import * as types from './types'

export const setHelloName = ({ commit }, name) => {
  commit(types.SET_HELLO_NAME, name)
}

export const setHelloName1 = ({ commit }, name) => {
  commit(types.SET_HELLO_NAME, name)
}
